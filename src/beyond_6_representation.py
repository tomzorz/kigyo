
# repr() is representation
# exactness is important, add all properties
# should be able to recreate class from it

# str() is friendly

# both call dunder versions of themselves

# str without overriding calls repr, so override that first!

# reprlib.repr is a friendly repr -> doesn't print millions of lines