# protocols are a set of operations a type must support to implement the protocol

# as opposed to c#, they don't need to be defined as an interface or base class

# we have "sized", "iterable" and more... we'll see later